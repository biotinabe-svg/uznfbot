name: Daily Quote to Telegram

on:
  schedule:
    # Har kuni soat 07:00 (Toshkent, UTC+5) = 02:00 UTC
    - cron: "0 2 * * *"
  workflow_dispatch: {}   # "Run workflow" tugmasi orqali qo'lda ham ishga tushirish mumkin

jobs:
  send-quote:
    runs-on: ubuntu-latest
    steps:
      - name: Repo-ni yuklab olish
        uses: actions/checkout@v4

      - name: Python o'rnatish
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Kutubxonalarni o'rnatish
        run: pip install -r requirements.txt

      - name: Iqtibosni yuborish
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
        run: python generate_and_send.py
